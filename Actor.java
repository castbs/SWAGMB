interface Actor {
    void move();      // Define movement behavior
    char getSymbol(); // Return the symbol representing the actor
    int getX();       // Get the X-coordinate of the actor
    int getY();       // Get the Y-coordinate of the actor
}
